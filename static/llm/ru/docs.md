# Документация ERA

URL: /ru/docs/

Точка входа в устройство платформы, структуру модов, игровые ресурсы и рабочие инструменты.

## С чего начать {#start}

Если вы впервые открыли ERA как автор мода, начните с **Быстрого старта**. Он отделяет безопасную подготовку рабочей копии игры от собственно редактирования и приводит к первому проверяемому результату. После него переходите к устройству мода и только затем — к конкретным форматам ресурсов.

Эта очередь важна: ERA загружает дополнительные компоненты, архивы и перенаправления во время запуска игры. Ошибка в структуре проекта поэтому часто выглядит как «файл не найден», хотя сам файл исправен.

## Разделы {#sections}

| Раздел | Что решает |
| --- | --- |
| Платформа ERA | Объясняет роль платформы и ключевые этапы запуска. |
| Установка | Помогает подготовить воспроизводимое окружение разработки. |
| Структура мода | Показывает, куда помещать данные, локали и ресурсы. |
| Игра и ресурсы | Даёт карту форматов и порядка поиска файлов. |
| Инструменты | Связывает тип задачи с подходящим классом редактора. |
| Глоссарий | Фиксирует русские и английские термины. |

## Как читать технические утверждения {#sources}

Каждая опубликованная статья связана с реестром источников. Для текущего поведения приоритет имеют исходники ERA, затем заметки конкретной версии и только затем старая справка. Рекомендации по рабочему процессу отмечаются как рекомендации и не подменяют подтверждённое поведение движка.

## Текущий объём {#scope}

Сейчас опубликован раздел общей документации. Разделы ERM-скриптов и плагинов намеренно не включены: они будут добавлены следующими самостоятельными очередями, не меняя основу сайта.

---

# Платформа ERA

URL: /ru/docs/era/

Роль ERA между Heroes III, модами, ресурсами и расширениями во время запуска игры.

## Что делает ERA {#role}

ERA расширяет процесс запуска и работы Heroes III: инициализирует собственные подсистемы, загружает настройки, создаёт виртуальный слой ресурсов и публикует события жизненного цикла. Для автора контентного мода главный практический результат — возможность держать изменения отдельно от оригинальных файлов игры.

Это не означает, что любой файл можно положить в произвольное место. Имя ресурса, поддерживаемый формат, каталог и порядок загрузки по-прежнему определяют, будет ли замена найдена.

## Жизненный цикл {#lifecycle}

Исходник `GameExt.pas` фиксирует последовательность внутренних этапов: ранняя инициализация, чтение настроек ERA, запуск виртуальной файловой системы, загрузка расширений и события до и после WoG. Названия внутренних событий полезны для проверки архитектуры, но не являются обещанием стабильного публичного API для обычного контентного мода.

## Виртуальные ресурсы {#vfs}

Менеджер архивов загружает дополнительные PAC-файлы из `Data`, ищет ресурсы в зарегистрированных архивах и применяет правила перенаправления. Отдельный менеджер ресурсов кэширует уже декодированные данные и учитывает ссылки на них. Поэтому «где хранится файл» и «какой ресурс сейчас использует игра» — связанные, но разные вопросы.

## Граница документации {#boundary}

Общая документация описывает установку, структуру модов, ресурсы и инструменты. Низкоуровневые детали движка приводятся только там, где они помогают объяснить наблюдаемое поведение и подтверждены исходниками. Сценарии ERM и API плагинов будут отдельными разделами.

---

# Совместимость и порядок модов

URL: /ru/docs/era/compatibility/

Как описывать зависимости, приоритет и конфликты без случайного порядка загрузки.

## Два направления порядка {#priority}

Документация SD Mod Manager предупреждает о важном различии: мод с большим приоритетом предоставляет ресурс раньше, но выражение «загружается после» описывает обратное направление списка. Если `Mod B` загружается после `Mod A`, то у `Mod B` выше приоритет и при одинаковом имени игра возьмёт его файл.

При диагностике всегда записывайте оба факта: видимый порядок модов и ожидаемого владельца конкретного ресурса. Фраза «мой мод последний» без названия менеджера и списка недостаточна.

## Поля compatibility {#fields}

| Поле | Смысл |
| --- | --- |
| `requires` | Моды, без которых запуск или назначение текущего мода не имеет смысла. Требование также влияет на порядок. |
| `load_after` | Моды с меньшим приоритетом; текущий мод должен перекрывать их совпадающие ресурсы. |
| `incompatible` | Сочетания, с которыми игра не должна запускаться или совместная работа не имеет смысла. |

Значения — стабильные имена каталогов модов. SD Mod Manager отдельно отмечает особое поведение `WoG`: он неявно добавляется в `requires` и `load_after`, если поля не переопределены. Проверяйте это правило на версии менеджера, которую распространяете с модом.

## Добавляйте правило только по причине {#declare}

Зависимость нужна, когда мод использует ресурс, событие или конфигурацию другого мода. `load_after` нужен при осознанном переопределении. `incompatible` нужен при подтверждённом конфликте, который нельзя безопасно разрешить порядком.

Не используйте длинный `load_after` как способ «всегда быть первым». Это делает сборку хрупкой и скрывает конфликт имён. Для каждого элемента списка запишите причину в документации проекта.

## Проверка матрицей {#matrix}

Проверьте как минимум четыре состояния: только базовая ERA; ERA плюс ваш мод; ваш мод плюс каждая обязательная зависимость; ваш мод вместе с наиболее вероятным конфликтующим модом в обоих допустимых порядках. Для каждого состояния используйте одну карту и один сценарий запуска.

Если проблема зависит от порядка, найдите точное имя ресурса, который предоставляют оба мода. Если проблема возникает до меню, сравните журналы чистого и конфликтного запуска. Не меняйте одновременно порядок, версию ERA и содержимое мода.

## Паспорт совместимости {#report}

Хороший отчёт содержит версию ERA, версию Mod Manager, точные имена и версии модов, порядок, минимальный сценарий воспроизведения и разницу журналов. Для бинарных изменений укажите версию исполняемого файла. Это превращает «не работает вместе» в проверяемый случай и позволяет добавить точное правило вместо догадки.

---

# Создание первого мода

URL: /ru/docs/era/creating-mod/

От пустого каталога до подключённого мода с метаданными, ресурсом и воспроизводимой проверкой.

## Подготовьте минимальный каталог {#directory}

Создайте для проекта отдельный каталог внутри `Mods`. Имя каталога должно быть стабильным: оно используется менеджером модов и в списках совместимости. Для первой проверки достаточно `Data`, а `Lang` и другие ветви добавляйте только тогда, когда они действительно нужны.

```text
Mods/MyMod/
├─ Data/
├─ Lang/
└─ mod.json
```

Не копируйте в мод весь каталог игры. Мод должен содержать только новые или заменяемые файлы. Тогда отключение каталога возвращает установку к исходному состоянию, а источник конфликта легче найти.

## Опишите мод в mod.json {#metadata}

SD Mod Manager документирует поля `name`, `description`, `author`, `homepage`, `support`, `icon`, `version`, `category` и `compatibility`. Поля типа `lng` допускают объект переводов: менеджер сначала ищет текущий язык и при отсутствии использует английский.

```json
{
  "name": { "ru": "Мой мод", "en": "My Mod" },
  "description": { "ru": "README.ru.md", "en": "README.en.md" },
  "author": "Author",
  "icon": "icon.png",
  "version": "0.1.0",
  "category": "gameplay"
}
```

Пути в `mod.json` считаются относительно каталога мода. Не добавляйте `requires`, `load_after` или `incompatible`, пока не можете назвать реальную зависимость или конфликт.

## Добавьте один проверяемый ресурс {#first-resource}

Для первого запуска выберите ресурс, результат которого легко увидеть и вернуть назад: строку локализации, изображение интерфейса или другой подтверждённый свободный файл. Сохраните точное имя и относительный путь. Регистр, расширение и вложенность важнее описательного имени файла.

ERA регистрирует поддерживаемые источники из `Data`, в том числе PAC-архивы, и применяет собственный слой поиска ресурсов. Поэтому случайное совпадение имени в другом активном моде способно скрыть ваш файл. На первом тесте оставьте включённым минимальный набор модов.

## Проверьте подключение {#enable-test}

1. Добавьте мод через используемый Mod Manager или в принятом вашей сборкой порядке модов.
2. Запустите тот же короткий сценарий, который проверяли до изменения.
3. Убедитесь, что изменился именно выбранный ресурс.
4. Полностью закройте игру перед повторным тестом, чтобы исключить уже загруженный ресурс из памяти.
5. Если эффекта нет, проверьте путь, имя, активность мода и конфликт с более приоритетным модом.

Не переходите к упаковке или массовой замене, пока один свободный файл не работает стабильно.

## Подготовьте проект к развитию {#project-hygiene}

Храните редактируемые исходники отдельно от экспортируемых игровых файлов. В репозиторий проекта полезно включить `mod.json`, тексты описания, исходники графики и сценарий сборки, но не временные файлы редакторов и не копию установленной игры. Версию мода меняйте осознанно, а каждую зависимость объясняйте в описании релиза.

---

# Установка и рабочая среда

URL: /ru/docs/era/installation/

Как подготовить воспроизводимую ERA-установку для разработки и не потерять рабочее состояние.

## Отдельная копия {#separate-copy}

Для разработки используйте отдельную копию игры. Это редакционная рекомендация, а не требование движка: её цель — сохранить рабочую игровую установку и дать вам стабильную точку сравнения.

Запишите версию ERA, источник дистрибутива и список активных модов. При совместной работе эти три значения часто важнее длинного описания «у меня не запускается».

## Проверка до изменений {#baseline}

1. Запустите игру без своего мода.
2. Откройте главное меню и загрузите тестовую карту.
3. Закройте игру штатно.
4. Сохраните появившиеся журналы как базовую точку сравнения.

ERA выполняет несколько этапов инициализации ещё до полной загрузки игрового интерфейса. Если чистый запуск нестабилен, собственный ресурс только усложнит диагностику.

## Рабочий цикл {#workflow}

Держите исходники мода отдельно от временных файлов инструментов. Копируйте в игровую папку только готовый результат или используйте воспроизводимый скрипт синхронизации. После каждой небольшой правки:

- зафиксируйте ожидаемое изменение;
- запустите один сценарий проверки;
- изучите журнал при ошибке;
- верните предыдущую версию, если причина неясна.

## Что не смешивать {#avoid}

Не обновляйте ERA, набор активных модов и свой ресурс одновременно. Различия версий могут менять поддерживаемые пути и форматы — например, локализация через `Lang/<language>` описана для конкретной линии ERA 3.9.6. Любую версионную возможность проверяйте по заметкам используемой сборки.

---

# Структура мода

URL: /ru/docs/era/mod-structure/

Практическая карта каталогов Data и Lang, архивов и правил приоритета ресурсов.

## Базовый каркас {#skeleton}

Мод храните в собственном каталоге под `Mods`. Не создавайте пустые ветви «на будущее»: добавляйте только те, которые нужны текущим ресурсам.

```text
Mods/MyMod/
├─ Data/        # игровые ресурсы и поддерживаемые архивы
└─ Lang/        # строки основной и дополнительных локалей
```

`Data` — не универсальная свалка. Вложенный путь должен соответствовать механизму, который загружает конкретный формат.

## Данные и архивы {#data}

Исходник ERA сканирует `Data/*.pac` и регистрирует найденные PAC-архивы. Для PNG-ресурсов ERA 3 также описывает ZIP-архивы внутри `Data`: их внутренняя структура должна выглядеть так, будто файлы лежат относительно корня игры. Реальные файлы в каталогах имеют приоритет над теми же путями внутри ZIP.

## Локализация {#lang}

Начиная с описанного в ERA 3.9.6 механизма, JSON-файлы из `Lang/<Language>` имеют приоритет над файлами в корне `Lang`. Практический шаблон: основную локаль держать в корне, альтернативные — в `Lang/en`, `Lang/ru` и других подпапках.

## Порядок и конфликт {#precedence}

Если результат не соответствует ожиданию, ищите конфликт по точному имени ресурса. Проверьте:

1. свободный файл в каталоге мода;
2. тот же путь внутри ZIP или PAC;
3. перенаправления в `Data/Redirections`;
4. другой активный мод, предоставляющий то же имя.

Не полагайтесь на случайный порядок файловой системы. Любое правило приоритета фиксируйте ссылкой на конкретную версию ERA или подтверждающий исходник.

---

# Устройство игры для автора мода

URL: /ru/docs/game/

Минимальная модель запуска, виртуальных ресурсов и кэша, достаточная для диагностики контентного мода.

## Полезная модель {#model}

Для большинства контентных задач не нужно знать все структуры памяти Heroes III. Достаточно различать четыре слоя: файлы игры, каталоги и архивы модов, слой поиска ERA и уже загруженные объекты в памяти.

Изменение файла влияет на следующий поиск ресурса, но не обязано мгновенно заменить объект, который игра уже декодировала и держит в кэше. Поэтому надёжная проверка начинается с полного перезапуска.

## Этапы запуска {#startup}

В исходнике ERA подсистемы инициализируются последовательными этапами: ранняя подготовка, настройки, виртуальная файловая система, дополнительные модули и события вокруг WoG. Ресурс может стать доступен только после того, как зарегистрирован соответствующий архив или правило перенаправления.

## Поиск ресурса {#lookup}

Менеджер LOD/PAC проходит по зарегистрированным архивам и умеет разрешать перенаправления имён. Конфигурации `Data/Redirections` формируют глобальные правила, включая отдельный режим для отсутствующих ресурсов. Для звука и видео проверка выполняется после загрузки медиасписков.

## Кэш и время жизни {#cache}

`ResLib.pas` описывает общий менеджер декодированных ресурсов: элементы индексируются относительным путём, имеют счётчик ссылок и участвуют в очереди очистки. Практический вывод прост: при проверке файловой замены перезапускайте игру, а не пытайтесь судить по экрану, который уже открыт.

## Где остановиться {#limits}

Если задача требует адресов памяти, бинарных патчей или вызовов внутренних функций, это уже не обычная замена ресурса. Такие изменения должны быть привязаны к точной версии исполняемого файла и будут документироваться отдельно вместе с плагинами.

---

# Как ERA загружает ресурсы

URL: /ru/docs/game/resource-loading/

Практическая модель VFS, архивов, перенаправлений и кэша для поиска конфликтов.

## От запроса игры к файлу {#pipeline}

Игра запрашивает ресурс по имени или относительному пути. ERA добавляет к исходным архивам зарегистрированные источники модов и проверяет правила перенаправления. Найденный файл затем декодируется соответствующим загрузчиком и может остаться в памяти через общий менеджер ресурсов.

Для диагностики полезно разделять три вопроса: какой путь запросила игра, какой источник выбрала ERA и удалось ли декодировать содержимое. Ошибка на любом этапе выглядит по-разному: ресурс может не находиться, проигрывать конфликт приоритета или находиться, но иметь неверный формат.

## Архивы и свободные файлы {#sources}

`Lodman.pas` регистрирует базовые LOD-архивы и сканирует `Data/*.pac`. ERA 3 также описывает ZIP-источники для PNG-замен. Внутренний путь архива должен соответствовать тому пути, который потребитель ожидает от корня игры или мода.

Свободный файл удобнее на этапе разработки: его можно заменить без перепаковки и сравнить побайтно. Архив имеет смысл после подтверждения имён и структуры. MMArchive отдельно предупреждает, что после замены или удаления записей архив фрагментируется; для VID особенно важна встроенная оптимизация перед работой другими утилитами.

## Перенаправления {#redirections}

Исходник ERA выделяет каталог `Data/Redirections` для глобальных конфигураций перенаправления. Перенаправление меняет цель поиска: это не копирование файла и не исправление содержимого. Отдельно обрабатываются случаи, когда исходный ресурс отсутствует, а для звука и видео часть разрешения выполняется после загрузки медиасписков.

Если используется перенаправление, записывайте исходное имя, целевое имя, конфигурационный файл и версию ERA. Цепочка из нескольких правил значительно усложняет диагностику; сначала проверьте одно правило без других модов.

## Приоритет и конфликт {#precedence}

Когда несколько активных модов предоставляют одинаковый путь, результат определяется их приоритетом. Порядок, видимый в менеджере модов, нужно трактовать по его документации: «загружается после» и «имеет больший приоритет» могут описывать противоположные направления списка.

Чтобы найти владельца результата, временно отключайте источники по одному или заменяйте ресурс легко узнаваемым тестовым вариантом. Не переименовывайте файл во время теста: вы должны исследовать одно и то же имя.

## Кэш и повторный тест {#cache}

`ResLib.pas` описывает индексирование декодированных ресурсов по относительному пути, счётчики ссылок и очередь очистки. Поэтому изменённый на диске файл не обязан немедленно заменить уже загруженный объект. Для контрольного теста полностью завершите игру и повторите тот же сценарий с чистого запуска.

Порядок проверки: подтвердить файл на диске, подтвердить активность мода, исключить более приоритетный дубль, проверить перенаправление, перезапустить игру, а затем исследовать структуру формата.

## Карта типичной неисправности {#failure-map}

| Симптом | Первый уровень проверки |
| --- | --- |
| Используется старый вариант | Кэш, приоритет другого мода, неверный путь |
| Пустое или розовое изображение | Размеры, палитра, прозрачность, индекс кадра |
| Игра падает при чтении | Структура файла, число записей, кодировка, несовместимая версия |
| Звук или видео не заменились | Медиасписок, имя, позднее перенаправление, формат контейнера |
| Работает только без другого мода | Совпадающий путь или явная несовместимость |

---

# Форматы ресурсов

URL: /ru/docs/game/resources/

Карта основных игровых и ERA-форматов, их назначения и безопасной проверки.

## Архивы {#archives}

| Формат | Роль |
| --- | --- |
| `LOD` | Базовый архив ресурсов Heroes III. ERA регистрирует несколько типов LOD для графики и звука. |
| `PAC` | Дополнительный архив, который ERA ищет в `Data/*.pac` и добавляет в список загрузки. |
| `ZIP` | В ERA 3 применяется для определённых PNG-ресурсов; внутренняя структура повторяет путь от корня игры. |

Для первой разработки предпочитайте свободный файл, когда механизм формата это допускает: его проще сравнить и заменить. Упаковывайте проверенный набор после того, как путь и имя уже подтверждены.

## Графика {#graphics}

`DEF` хранит группы кадров игровой анимации. В ERA 3 отдельные кадры могут заменяться PNG по шаблону `Data/Defs/<name.def>/<group>_<frame>.png`; индексы группы и кадра начинаются с нуля. Для некоторых интерфейсных PCX предусмотрены PNG-замены в `Data/Pcx`.

Сохраняйте размеры, смысл прозрачности и порядок кадров. Внешне «правильный» PNG всё ещё может быть неверным ресурсом, если путь, группа или индекс не совпадают.

## Текст и данные {#text-data}

- `TXT` — таблицы и строки классических игровых ресурсов; структура строк и разделителей зависит от конкретного файла.
- `ERT` и `ERS` — связанные со старой ERM-экосистемой текстовые и ресурсные таблицы; используйте их только с документацией конкретного механизма.
- `JSON` — конфигурация ERA, локализация и перенаправления. Схема определяется потребителем файла, а не расширением.

Всегда сохраняйте кодировку, число столбцов и управляющие последовательности исходного формата.

## Звук, видео и шрифты {#media}

Классические ресурсы включают WAV-звуки, видеоролики и игровые шрифты. ERA различает поиск данных в архивах и медиасписках; перенаправление отсутствующего WAV или видео может применяться позже обычного LOD-поиска.

## Диагностика {#diagnostics}

Проверяйте одну замену за раз. Если ресурс не найден, последовательно проверьте имя, путь, архив, перенаправление и конфликт модов. Если найден, но отображается неверно — проверяйте структуру самого формата.

---

# Глоссарий

URL: /ru/docs/glossary/

Согласованные термины общей документации ERA на русском и английском.

## Платформа и проект {#platform}

| English | Русский | Значение |
| --- | --- | --- |
| ERA platform | платформа ERA | Набор расширений и механизмов загрузки вокруг Heroes III/WoG. |
| mod | мод | Отдельный набор данных и поведения, подключаемый к игре. |
| mod directory | каталог мода | Корневой каталог конкретного мода внутри `Mods`. |
| game root | корень игры | Каталог установки Heroes III, относительно которого описываются пути. |

## Ресурсы {#resources}

| English | Русский | Значение |
| --- | --- | --- |
| resource | ресурс | Именованные данные, которые игра загружает: изображение, текст, звук и т. п. |
| loose file | свободный файл | Файл в обычном каталоге, а не внутри архива. |
| archive | архив | Контейнер ресурсов, например LOD или PAC. |
| redirection | перенаправление | Правило, которое связывает запрошенное имя ресурса с другим путём. |
| load order | порядок загрузки | Последовательность, влияющая на выбор ресурса при совпадении имён. |

## Технические термины {#technical}

`LOD`, `PAC`, `DEF`, `TXT`, `JSON`, `ERT`, `ERS`, `PNG`, `WAV` и имена каталогов не переводятся. В русском тексте допустимо склонять родовое слово: «архив PAC», «кадр DEF», «файл JSON».

Термины `trigger`, `receiver` и `ERM command` зарезервированы для будущего раздела ERM; их технические коды также не переводятся.

---

# Карта материалов для LLM

URL: /ru/docs/llm-map/

Человекочитаемый указатель страниц и ссылки на компактные машинные представления.

## Как использовать {#usage}

Начните с `/llms.txt` для краткой карты, затем откройте `/llm/manifest.json`, если нужны стабильные ID, локали, ключевые слова, вопросы и связи. Полный текст опубликованных статей доступен в `/llm/ru/docs.md` и `/llm/en/docs.md`.

## Ограничения {#limits}

ERM-скрипты и плагины не входят в текущий manifest. Не выводите их отсутствие как ошибку покрытия: эти разделы намеренно отложены. Техническое утверждение следует связывать с `sourceRefs`, а не выводить только из заголовка или ключевого слова.

## Машинные файлы {#machine-files}

- `/llms.txt` — короткая точка входа;
- `/llms-full.txt` — полный компактный каталог;
- `/llm/manifest.json` — структурированные сущности;
- `/llm/{lang}/docs.md` — содержимое документации по локали.

## Автоматический каталог {#catalog}

Ниже сборка выводит каждую опубликованную сущность текущей локали: стабильный ID, каноническую ссылку, краткое описание, вопросы и ключевые слова. Каталог формируется из того же реестра, что поиск и manifest, поэтому новая опубликованная страница не требует ручного добавления в этот список.

---

# Быстрый старт

URL: /ru/docs/quick-start/

Безопасная последовательность от рабочей копии игры до первого проверенного ресурса мода.

## 1. Подготовьте окружение {#prepare}

Используйте отдельную рабочую копию Heroes III с ERA, а не единственную игровую установку. Сохраните исходное состояние и записывайте версию платформы: это позволит отличить ошибку проекта от различий между сборками.

После установки запустите игру без собственных изменений. Если чистый запуск не работает, не переходите к моддингу — сначала восстановите базовое окружение.

## 2. Создайте минимальный мод {#skeleton}

Создайте отдельный каталог проекта внутри каталога модов. Минимальная структура зависит от типа первого ресурса, но полезно сразу отделить данные и локализацию:

```text
Mods/
└─ MyFirstMod/
   ├─ Data/
   └─ Lang/
```

Не изменяйте оригинальные архивы игры для первой проверки. Отдельный каталог легче отключить, сравнить и удалить без побочных эффектов.

## 3. Выберите маленькую проверку {#first-change}

Хороший первый шаг — один заменяемый текстовый или графический ресурс, имя и путь которого вы точно знаете. Меняйте только одну вещь за запуск. Так вы проверяете сразу три условия: мод активен, путь корректен, а формат читается игрой.

## 4. Проверьте результат {#verify}

Перед запуском зафиксируйте ожидаемый эффект. После запуска проверьте именно его, затем закройте игру и изучите журналы ERA при наличии ошибки. Если результат не появился:

1. проверьте точное имя и регистр файла;
2. проверьте вложенность каталогов;
3. убедитесь, что другой мод не переопределяет тот же ресурс;
4. временно отключите несвязанные моды и повторите запуск.

## Следующий шаг {#next}

Перейдите к **структуре мода**, чтобы понять каталоги `Data` и `Lang`, архивы и правила приоритета. До устойчивой первой замены не имеет смысла одновременно подключать сложные инструменты и несколько форматов.

---

# Структура сайта

URL: /ru/docs/site-structure/

Как устроены маршруты, локали, поиск, источники и машинные представления документации.

## Маршруты и язык {#routes}

Язык всегда идёт первым сегментом URL: `/ru/…` или `/en/…`. Переключатель языка сохраняет текущую страницу и фрагмент, поэтому ссылка на конкретный раздел остаётся осмысленной в обеих локалях.

Главные коллекции представлены короткими каталогами, а не сотнями пунктов в боковом меню. Текущая версия сайта содержит только общую документацию; ERM и плагины будут подключены позднее через тот же реестр.

## Навигация {#navigation}

Слева находится редакционное дерево глубиной не более двух уровней. В центре — статья. Справа — оглавление текущей страницы. На мобильном устройстве левое меню открывается как панель, а оглавление остаётся частью документа.

## Контент и локализация {#content}

Нелокализуемые поля статьи — стабильный ID, slug, ключевые слова, вопросы и источники — хранятся в `entity.json`. Длинный текст живёт в паре `ru.md` / `en.md`. Заголовки локалей используют одинаковые section ID, например `{#resources}`.

## Поиск {#search}

Короткий индекс заголовков и ключевых слов открывается сразу, а полнотекстовый индекс Pagefind строится из уже сгенерированного HTML. Индексы локалей разделены, чтобы русские и английские результаты не смешивались.

## Проверяемость {#provenance}

В `content/_sources/sources.json` находится компактный реестр происхождения. Исходные архивы, changelog и код ERA остаются внешними материалами разработки и не попадают в публикуемый сайт.

---

# Инструменты моддера

URL: /ru/docs/tools/

Как выбрать программу под формат, отделить редактирование от проверки и безопасно работать со старым комплектом Tools.

## Начинайте с формата {#choose}

Инструмент выбирают не по известности, а по паре «вход → выход». Запишите имя и формат исходного файла, ожидаемый результат, версию программы и способ проверки в игре. Если эти четыре пункта неизвестны, запуск старой утилиты создаёт больше неопределённости, чем пользы.

Комплект ERA содержит современные и унаследованные программы рядом. Одинаковая задача может иметь несколько решений: например, архивы обрабатываются MMArchive и отдельными `lod*.exe`, а шрифты — двумя версиями Font Editor. Для нового процесса выбирайте вариант с понятной встроенной справкой и обратимой операцией.

## Рабочая цепочка по типу данных {#workflow}

| Задача | Основной инструмент | Независимая проверка |
| --- | --- | --- |
| LOD/PAC/VID и связанные архивы | MMArchive | извлечение, обратный импорт, сравнение и запуск |
| Просмотр DEF | Def Preview | группы, кадры и тип DEF |
| Сборка DEF | Heroes3 Def Tool | повторное открытие и проверка в игре |
| TXT-таблицы | Txt Tables Editor | кодировка, колонки, разделители |
| Файлы `object` | Object TXT Files Editor | одна скопированная таблица и тестовая карта |
| Шаблоны случайных карт | Template Editor | экспорт/импорт `rmg.txt` и генерация карты |
| BIK/SMK | RS Bink Player | воспроизведение и покадровый просмотр |
| Список и порядок модов | SD Mod Manager | контрольный запуск и проверка владельца ресурса |

Подробные названия всех каталогов и исполняемых файлов находятся в [каталоге инструментов](catalog/).

## Архивы требуют round-trip {#archives}

MMArchive документирует добавление файлов, объединение архивов и оптимизацию. Замена и удаление создают фрагментацию; для VID программа хранит дополнительную информацию о размерах, которую другие инструменты могут не распознать до оптимизации.

Перед массовым изменением извлеките один ресурс, импортируйте его без смысловой правки и сравните результат. Успешный round-trip подтверждает только техническую цепочку — после него всё равно нужен запуск игры.

## Графика, палитры и интерфейс {#graphics}

Def Preview подходит для быстрого просмотра анимационных групп. Heroes3 Def Tool предназначен для сборки и редактирования DEF, включая кадры, маски и тени. DEFka поставляется отдельно для графики существ, а Heroes 3 Dialogs — как редактор пользовательских диалогов WoG. Font Editor представлен двумя версиями.

Не переносите параметры «по похожей картинке». Размеры, индекс группы, номер кадра, палитра и прозрачность являются частью ресурса. PngCrush уменьшает PNG, но не исправляет неверный путь или смысл альфа-канала.

## Инструменты повышенного риска {#risk}

BinMagic, ExeBuilder, EVME и UN:C to BIN работают с патчами, исполняемым файлом или памятью процесса. Их результат зависит от конкретной версии EXE и может повредить файл или запущенную игру. Этот сайт пока не публикует инструкции ERM/UN:C: наличие утилиты в комплекте не означает, что адрес или рецепт подходит текущей сборке.

Используйте копию, храните контрольную сумму оригинала и никогда не применяйте неизвестный патч «для проверки». EVME прямо описывает потенциально опасные операции записи памяти и дампа.

## Безопасный процесс {#safe-process}

1. Скопируйте исходник и зафиксируйте его SHA-256.
2. Выполните одну операцию одним инструментом.
3. Сохраните экспорт отдельно от редактируемого исходника.
4. Откройте результат второй программой, если формат это допускает.
5. Проверьте один повторяемый сценарий в отдельной установке игры.
6. Только после этого обрабатывайте пакет файлов.

Если встроенной справки нет, ограничьтесь инвентаризацией программы и найдите подтверждённую документацию для точной версии. Не угадывайте параметры командной строки по имени EXE.

---

# Каталог инструментов ERA

URL: /ru/docs/tools/catalog/

Проверенный список программ из комплекта Tools, сгруппированный по задаче и уровню риска.

## Как читать каталог {#usage}

Названия ниже соответствуют каталогам и исполняемым файлам установленного комплекта ERA Tools. Описание приводится только там, где его подтверждает встроенная справка, метаданные EXE или состав поставки. Наличие программы не гарантирует совместимость с каждой версией ERA и Windows: работайте на копии игры и сохраняйте исходный файл.

## Моды, редактор и диагностика {#modding}

| Инструмент | Назначение и примечание |
| --- | --- |
| **Mod Manager / SD Mod Manager** | Управление набором модов и их порядком. Встроенная документация описывает `mod.json`, локализуемые поля и `requires`, `load_after`, `incompatible`. В комплекте есть основная, старая и тёмная сборки; не смешивайте их настройки. |
| **Erm_Editor_Sublime** | Поставляемая конфигурация Sublime Text с пакетами для ERM. Сам справочник ERM отложен, но редактор остаётся рабочим местом для текстов сценариев. |
| **Issue Wizard** | В поставке присутствует `Issue Wizard.exe` и настройки репозитория. Используйте конкретную инструкцию вашей сборки; публичная документация комплекта не описывает его протокол полностью. |
| **DebugCreator** | Каталог содержит `DebugCreator.hta` и файл настроек. Назначение и совместимость в поставке не документированы достаточно подробно для пошаговой инструкции. |
| **VfsTest** | Отдельный `VfsTest.exe` с тестовыми каталогами. Это специализированная проверочная утилита; запускайте её только на копии установки и сверяйте с версией ERA. |
| **Autoit** | Среда выполнения AutoIt, используемая некоторыми вспомогательными сценариями комплекта. Это не редактор игрового формата. |

## Архивы и таблицы {#archives-tables}

| Инструмент | Назначение и примечание |
| --- | --- |
| **MMArchive 1.3.1** | Редактирует архивы Might & Magic 6–8 и Heroes III, кроме `*.hwl`. Умеет добавлять файлы, объединять архивы и оптимизировать их после замен/удалений. Для VID встроенная оптимизация особенно важна перед использованием других программ. |
| **Txt Tables Editor 1.4.1** (`TextEdit/TxtEdit.exe`) | Редактор табличных TXT-ресурсов. Сохраняйте исходную кодировку, число колонок и разделители. |
| **Object TXT Files Editor 1.5** (`ObjTxtEditor/objtxteditor.exe`) | По метаданным EXE — редактор файлов `object` Heroes III. Проверяйте результат на одной копии таблицы. |
| **lodexport.exe / lodimport.exe / lodmerge.exe** | Отдельные консольные утилиты поставки для операций с LOD. Локальная поставка не содержит рядом достаточной справки, поэтому для новых задач предпочтительнее документированный MMArchive. |
| **resman.exe / repman.exe / objector.exe** | Унаследованные отдельные утилиты в корне Tools. Их интерфейсы и допустимые версии входов в комплекте не описаны; не применяйте к единственной копии ресурса. |

## DEF, интерфейс и шрифты {#graphics}

| Инструмент | Назначение и примечание |
| --- | --- |
| **Def Preview 1.0** | Просмотр анимации выбранной группы DEF. Встроенная справка также описывает переименование групп, изменение типа DEF через строку состояния и загрузку номеров групп из другого DEF. |
| **Heroes3 Def Tool 3.2.1** (`H3DefTool`) | Сборка и редактирование DEF, кадров, масок и теней. Встроенный учебник описывает создание нового DEF и резервные копии; всегда проверяйте палитру и индексы кадров. |
| **DEFka 2.0** | Отдельная программа для работы с наборами графики существ. Поставка содержит базу данных и служебные текстовые файлы, но не даёт полной современной спецификации формата. |
| **Heroes 3 Dialogs** (`Dialogs Editor`) | Метаданные EXE называют программу редактором пользовательских диалогов WoG. ERM-механизм диалогов будет описан позже; здесь фиксируется только назначение инструмента. |
| **FNT Editor / FNTeditor** (`Font Editor`) | Две поставляемые версии редактора игровых шрифтов. Не перезаписывайте единственный FNT-файл и проверяйте полный набор символов обеих локалей. |
| **PngCrush** | Поставка `pngcrush.exe` и командного файла для оптимизации PNG. Оптимизация не исправляет неверный путь, размер или семантику прозрачности. |

## Карты и шаблоны {#maps}

**Template Editor 0.99** (`TEditor`) работает с шаблонами генератора случайных карт. Его справка описывает создание и импорт шаблонов из `rmg.txt`, зоны, переходы, условия существования, охрану и файлы проекта `.tpz`. Для импорта встроенных шаблонов требуется извлечённый `rmg.txt` из `h3bitmap.lod`.

Каталог **ExeMapCompiler** содержит сценарии `compile.phc`, `decompile.phc`, `optimize.phc` и `mapToDbgmap.bat`; пошаговая справка в поставке отсутствует. Используйте только в процессе, где формат входа уже подтверждён.

## Видео и мультимедиа {#media}

| Инструмент | Назначение и примечание |
| --- | --- |
| **RS Bink Player 1.2.5** | Проигрывает `*.bik` и `*.smk`, в том числе ролики Heroes III. Справка описывает переход между файлами, паузу, покадровую навигацию и сохранение кадров. |
| **Smacker** | Набор RAD Video Tools/Smacker для обработки соответствующего видеоформата. Это сторонний исторический набор: прежде чем конвертировать материал, сохраните исходник и проверьте права на него. |

## Патчи и специализированные конвертеры {#patches}

| Инструмент | Назначение и уровень риска |
| --- | --- |
| **BinMagic 1.0** | Создаёт, преобразует и применяет текстовые или бинарные патчи. Поддерживает преобразование адресов через DLL. Ошибка адреса или версии повреждает целевой файл — всегда работайте с копией. |
| **ExeBuilder** | Берёт `in.exe`, применяет все найденные рядом `*.bin` и создаёт `out.exe`. Справка состоит из этой краткой схемы; не используйте результат с другой версией EXE. |
| **EVME 1.0 / ERM Vars Memory Editor** | Отладчик переменных и памяти запущенной игры для старых линий WoG/TE/Phoenix. Встроенная справка прямо предупреждает об опасных операциях записи памяти и дампов; для современной сборки совместимость не предполагается. |
| **UN:C to bin patch converter** | Преобразует последовательность записей `C[address]/size/value` в один BIN-патч. Это инструмент бинарных адресов; подробности UN:C отложены вместе с разделом ERM. |
| **ScriptsToEra260** | Каталог конвертера сценариев с PHP-скриптом и таблицей именованных функций. Пошаговое применение относится к будущей ERM-документации. |
| **installmod.exe** | Корневая утилита установки мода. Публичной встроенной справки рядом нет; Mod Manager предпочтительнее для контролируемого процесса. |

## Правило безопасного выбора {#safety}

Сначала выберите формат, затем инструмент, затем способ проверки. Сохраните оригинал, выполните одно действие, сравните результат и только после запуска в игре переходите к пакетной обработке. Для утилит без встроенной справки каталог подтверждает наличие, но не даёт права угадывать ключи командной строки или совместимость.

---

# Диагностика проблем

URL: /ru/docs/troubleshooting/

Повторяемая схема от чистого запуска до точного отчёта о конфликте или неверном ресурсе.

## Зафиксируйте базовое состояние {#baseline}

До исправлений запишите версию ERA, версию игры, активные моды и их порядок. Запустите игру без разрабатываемого мода, откройте тестовую карту и сохраните журналы. Эта контрольная точка отделяет проблему установки от проблемы нового контента.

Если чистый запуск уже нестабилен, временно остановите работу над ресурсом. Обновление платформы, изменение списка модов и правка файла в одном тесте создают три независимые причины.

## Сведите случай к одному изменению {#minimal-case}

Оставьте один мод, один изменённый ресурс и один короткий сценарий. Для графики это один экран или объект, для таблицы — одна строка, для порядка модов — одна пара конфликтующих каталогов. Полностью перезапускайте игру между испытаниями.

Сравнивайте не только экран, но и точный путь файла, размер, время изменения и контрольную сумму. Если свободный файл работает, а архивный нет, проблема находится в упаковке или внутреннем пути, а не в содержимом ресурса.

## Разделите типы ошибок {#classify}

1. **Файл не выбран.** Проверьте имя, путь, активность мода, приоритет и перенаправления.
2. **Файл выбран, но не читается.** Проверьте формат, кодировку, размеры, число записей и ограничения конкретного загрузчика.
3. **Файл прочитан, но эффект не обновился.** Исключите кэш и уже созданные игровые объекты полным перезапуском.
4. **Проблема зависит от сочетания модов.** Найдите общий путь и проверьте оба порядка.
5. **Падение происходит до меню.** Сравните журналы и верните последнее независимое изменение.

## Используйте подходящий инструмент {#tools}

Для архивов делайте пробное извлечение и обратный импорт через MMArchive. DEF сначала просматривайте в Def Preview, а затем проверяйте сборку в Heroes3 Def Tool. Таблицы редактируйте в Txt Tables Editor или Object TXT Files Editor только после сохранения исходной кодировки и структуры. `VfsTest.exe` присутствует в комплекте инструментов как отдельная утилита; её точную совместимость с вашей сборкой нужно проверять на копии установки.

Инструмент не заменяет контрольный сценарий. Успешное открытие файла доказывает лишь то, что конкретная программа смогла его прочитать.

## Составьте полезный отчёт {#report}

Укажите версии, список и порядок модов, точный путь изменённого файла, ожидаемый и фактический результат, минимальные шаги и релевантный фрагмент журнала. Приложите контрольную сумму небольшого собственного тестового файла вместо всей игры или чужого дистрибутива. Для бинарной правки обязательно укажите версию EXE и способ применения.

Хороший отчёт позволяет другому человеку повторить ошибку без догадок и проверить одно утверждение за один запуск.
